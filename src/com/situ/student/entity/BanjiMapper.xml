<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.situ.student.dao.IBanjiDao2">
	<select id="findStudentByBanji" parameterType="int" resultType="Student">
		SELECT *FROM student WHERE banji_id=#{id}
	</select>
	
</mapper>
